This workspace is an integration fixture for `jturbide-gulp`. It builds a small
localized static site and exercises Sass, JavaScript, views, image processing,
and file-copy tasks.

Run it from the repository root with:

```sh
pnpm test:integration
```
